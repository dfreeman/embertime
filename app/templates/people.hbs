{{!
  ! The default template for a route or component when none is specified
  ! is just an outlet with nothing else around it, so we could safely
  ! delete this file if we wanted to without changing anything.
  !}}

{{outlet}}
