import Ember from 'ember';

/*
 * The application route serves as the root of our app; it's always active
 * when our app is running.
 *
 * This route is now empty, because we want to load and display different
 * information depending on what route we're on. We could delete this file
 * if we wanted, though the application route can end up being a kind of
 * hub for integrations in more complex applications, so we'll keep it around.
 */
export default Ember.Route.extend({
});
