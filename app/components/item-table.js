import Ember from 'ember';

/*
 * This file was created when we did `ember g component item-table`.
 * Since it doesn't actually override any default component behavior,
 * we could also delete the file entirely and not affect anything.
 *
 * The interesting parts of this component are in its template
 * (app/templates/components/item-table.hbs)
 */
export default Ember.Component.extend({
});
